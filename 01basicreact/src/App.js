import Chai from './chai.js'

function App() {
  return (
    <><Chai/>
    <h3>This is called a component</h3>
    <p>In vite and basic React we need to write a program having capital letter first in the name, </p>
    <p>add it to the app function after exporting and importing the file and render the app via index file</p>
    </>
    
  );
}

export default App;
